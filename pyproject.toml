[project]
name = "workoutloud"
version = "0.1.0"
description = "Automation utilities for managing branching workflows"
authors = [{name = "Automation"}]
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
